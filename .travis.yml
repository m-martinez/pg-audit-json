# Global Config
language: python
sudo: required

services:
  - postgresql

addons:
  postgresql: "9.6"
  apt:
    packages:
      - postgresql-server-dev-9.6

before_script:
  - sudo make install

script:
  - sudo make installcheck
